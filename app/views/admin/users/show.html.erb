<div style = "padding: 15px 0px">
  <div class = "container">
    <div class="card-body">
      <div class="row">
        <div class="col-sm-6">
          <div class="card text-white bg-primary mb-3">
            <%= render "user_card", user: @user %>
          </div>
        </div>
      </div>
      <%= render "order", orders: @orders %>
      <%= render "rating", ratings: @ratings %>
      <%= link_to t("admin.user.back"), admin_users_path, class: "btn btn-outline-primary"%>
    </div>
  </div>
</div>
